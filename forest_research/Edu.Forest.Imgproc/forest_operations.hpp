#pragma once

namespace fr {

/************************************************************************/
/*                        fast abs                                      */
/************************************************************************/

inline int abs_i32(int n) 
{
	int mask = (n >> (sizeof(int) * 8 - 1));
	return (n + mask) ^ mask;
}

}